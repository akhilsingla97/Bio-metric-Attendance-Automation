<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akhilsingla.biometricattendancesystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Finger-Print Attendance Automation"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signin_activity" />
        <activity android:name=".signup_activity" />
        <activity android:name=".after_signin_activity" />
        <activity android:name=".after_signup_activity" />
        <activity android:name=".add_courses" />
        <activity android:name=".Settings" />
        <activity android:name=".Admin_signin" />
        <activity android:name=".after_admin_signin"></activity>
    </application>

</manifest>